default_platform(:ios)

platform :ios do
  desc "Push a new beta build to TestFlight"
  lane :beta do
    # API Key setup
    api_key = app_store_connect_api_key(
      key_id: ENV["APP_STORE_CONNECT_KEY_ID"],
      issuer_id: ENV["APP_STORE_CONNECT_ISSUER_ID"],
      key_content: ENV["APP_STORE_CONNECT_KEY_CONTENT"],
      is_key_content_base64: false # We will store the P8 content as raw text in secrets
    )

    # Clean previous builds
    clear_derived_data
    
    # Fetch/Create Certificate
    get_certificates(
      api_key: api_key,
      output_path: "fastlane/certificates",
      development: false
    )

    # Fetch/Create Provisioning Profile
    get_provisioning_profile(
      api_key: api_key,
      development: false,
      app_identifier: "com.metestudios.flowifygame",
      output_path: "fastlane/profiles"
    )

    # Set Team ID in the project file
    update_project_team(
      path: "App.xcodeproj",
      team_id: "ERUH5NNQUB"
    )

    # Force Pods to use the correct team or skip signing for resource bundles
    # This fixes the "requires a development team" error for Google Ads bundles
    sh("sed -i '' 's/DEVELOPMENT_TEAM = \"\";/DEVELOPMENT_TEAM = \"ERUH5NNQUB\";/g' Pods/Pods.xcodeproj/project.pbxproj || true")

    # Build the app - gym will find the profile automatically
    gym(
      scheme: "App",
      workspace: "App.xcworkspace",
      output_directory: "build",
      output_name: "App.ipa",
      include_bitcode: false,
      export_method: "app-store",
      xcargs: "CODE_SIGNING_REQUIRED=YES CODE_SIGNING_ALLOWED=YES",
      export_options: {
        method: "app-store",
        provisioningProfiles: {
          "com.metestudios.flowifygame" => "com.metestudios.flowifygame AppStore"
        }
      }
    )

    # Upload to TestFlight
    pilot(
      api_key: api_key,
      ipa: "build/App.ipa",
      skip_waiting_for_build_processing: true
    )
  end
end
